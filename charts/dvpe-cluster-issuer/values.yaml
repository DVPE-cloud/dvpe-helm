clcm:
  # clcm.host -- CLCM host
  host:
  # clcm.port -- CLCM port
  port:
  # clcm.healthCheckTimeoutInSeconds -- CLCM health check interval in seconds
  healthCheckTimeoutInSeconds: 60
  # clcm.secretsNamespace -- Namespace, where secrets are deployed
  secretsNamespace:
# internet -- configuration for the internet cluster issuer
issuers:
  # internet -- configuration for the internet cluster issuer
  internet:
    # -- Secret containing CLCM connect credentials
    credentialsSecret:
    # -- CA instance
    caInstance:
    # -- CA type
    certificateDateCAType:
    # -- CI id
    ciID:
    # -- CI type
    ciType:
    # -- CI client
    ciClient:
    # -- CI contact e-mail
    ciContactEmail:
    # -- CI requester
    ciRequester:
