---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.12.0
  name: issuers.cert-manager.pki.bmwgroup.net
spec:
  group: cert-manager.pki.bmwgroup.net
  names:
    kind: Issuer
    listKind: IssuerList
    plural: issuers
    singular: issuer
  scope: Namespaced
  versions:
    - name: v1alpha1
      schema:
        openAPIV3Schema:
          description: Issuer is the Schema for the issuers API
          properties:
            apiVersion:
              description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
              type: string
            kind:
              description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
              type: string
            metadata:
              type: object
            spec:
              description: IssuerSpec defines the desired state of Issuer
              properties:
                appdId:
                  type: string
                authSecretName:
                  description: AuthSecretName is a reference to a secret in the same
                    namespace as the referent. This secret contains the credentials
                    for accessing the CARDS API endpoint.
                  type: string
                caType:
                  type: string
                cardsHost:
                  default: cards.bmwgroup.net
                  description: 'CardsHost is the base host for the endpoint of the signing
                  service, for example: "cards-domain.example.com".'
                  type: string
                cardsPort:
                  default: 443
                  type: integer
                department:
                  type: string
                itsmService:
                  type: string
                teamEmail:
                  type: string
                userEmail:
                  type: string
                userId:
                  type: string
                wenHost:
                  default: auth.bmwgroup.net
                  description: 'WenHost is the host for getting access-token, which
                  is further used to get sessionId, for example: "wen-domain.example.com".'
                  type: string
                wenPort:
                  default: 443
                  type: integer
              required:
                - authSecretName
              type: object
            status:
              description: IssuerStatus defines the observed state of Issuer
              properties:
                conditions:
                  description: List of status conditions to indicate the status of a
                    CertificateRequest. Known condition types are `Ready`.
                  items:
                    description: IssuerCondition contains condition information for
                      an Issuer.
                    properties:
                      lastTransitionTime:
                        description: LastTransitionTime is the timestamp corresponding
                          to the last status change of this condition.
                        format: date-time
                        type: string
                      message:
                        description: Message is a human readable description of the
                          details of the last transition, complementing reason.
                        type: string
                      reason:
                        description: Reason is a brief machine readable explanation
                          for the condition's last transition.
                        type: string
                      status:
                        description: Status of the condition, one of ('True', 'False',
                          'Unknown').
                        enum:
                          - "True"
                          - "False"
                          - Unknown
                        type: string
                      type:
                        description: Type of the condition, known values are ('Ready').
                        type: string
                    required:
                      - status
                      - type
                    type: object
                  type: array
              type: object
          type: object
      served: true
      storage: true
      subresources:
        status: {}
